# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
sqlalchemy==2.0.31
asyncpg==0.29.0
alembic==1.13.2

# Task queue
celery==5.4.0
redis==5.0.7

# LLM / RAG
langchain==0.2.12
langchain-openai==0.1.20
langchain-community==0.2.11
langchain-text-splitters==0.2.4
langgraph==0.1.19
openai==1.37.1
rank-bm25==0.2.2

# Evaluation
ragas==0.1.14
datasets==2.20.0

# ML classifier
xgboost==2.0.3
scikit-learn==1.5.1

# Vector DB
qdrant-client==1.10.1

# Document parsing
pymupdf==1.24.7
pdfplumber==0.11.2
unstructured[pdf,docx]==0.15.0
python-docx==1.1.2
openpyxl==3.1.5


# Utilities
httpx==0.27.0
python-multipart==0.0.9
structlog==24.4.0
tenacity==8.4.2
numpy>=1.26.0
psycopg2-binary>=2.9.9
aiofiles>=23.2.1

# Production
slowapi==0.1.9
sentry-sdk[fastapi]==2.8.0

# ── Extension 1: PyTorch DistilBERT Failure Classifier ──────────────────────
torch>=2.3.0
transformers>=4.42.0
# tensorboard>=2.17.0  # optional: training visualisation

# ── Extension 2: TensorRT + Triton inference optimisation ────────────────────
onnx>=1.16.0
onnxruntime>=1.18.0
# tensorrt — install from NVIDIA .whl, not PyPI (requires CUDA driver)
# pycuda>=2024.1   — required for TensorRT benchmarking
# pynvml>=11.5.0   — GPU auto-profiler (ships with nvidia-ml-py)
tritonclient[grpc]>=2.47.0

# ── Extension 3: Voice interface ─────────────────────────────────────────────
openai-whisper>=20231117
sounddevice>=0.4.7
soundfile>=0.12.1
# TTS>=0.22.0       — Coqui TTS (large install, optional)
# pyttsx3>=2.90     — lightweight TTS fallback
librosa>=0.10.2

# ── Extension 4: Domain-Adaptive Embedding Fine-Tuning ───────────────────────
sentence-transformers>=2.2.0
